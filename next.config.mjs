/** @type {import('next').NextConfig} */
const nextConfig = {
   
        // ...other configurations
        sassOptions: {
          includePaths: ['styles'],
        },

      
};

export default nextConfig;
